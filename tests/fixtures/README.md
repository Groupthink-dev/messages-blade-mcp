# Test Fixtures

Sample `attributedBody` blobs for testing the typedstream decoder.
These are synthetic fixtures that mimic the NSArchiver format used by Messages.app on macOS Ventura+.

Real chat.db data is never committed to this repository.
